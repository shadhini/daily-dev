`zsh` is the default shell on macOS, replacing bash

## why is zshrc not available on new macbook pro M5 
- macOS doesn’t create a per-user `~/.zshrc` by default
- Apple ships system-wide zsh startup files (in /etc) and leaves your home directory empty so user configuration only appears when you create it



