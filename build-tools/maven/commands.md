# Maven Commands

check the maven version and Java version it uses (set by JAVA_HOME environment variable)
    
    mvn -v

add a maven wrapper to a project using locally installed maven

    mvn wrapper:wrapper

build the project using maven wrapper
    
    ./mvnw clean install
